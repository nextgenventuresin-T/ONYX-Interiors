# Copy to .env (or set in your host's dashboard) to point the frontend at
# your deployed backend. Leave unset for local dev (Vite proxies /api).
# Example:
# VITE_API_URL=https://atelier-api.onrender.com
VITE_API_URL=
